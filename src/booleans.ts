/** booleans */

interface User {
  isAdmin: boolean; // purrrfect!
  hasAvatar: boolean; // purrrfect!
  isNotMale: boolean; // Do not use negative names
  userWearsGlasses: boolean; // Do not use redundant context
  facialHairFlag: boolean; // Do not ignore the convention to form a statement using verb + subject
}